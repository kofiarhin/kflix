const COOKIE_NAME = 'token';

const VALID_CONTENT_TYPES = ['movie', 'tv', 'both'];
const VALID_DISCOVERY_STYLES = ['popular', 'top_rated', 'new'];

module.exports = {
  COOKIE_NAME,
  VALID_CONTENT_TYPES,
  VALID_DISCOVERY_STYLES,
};
